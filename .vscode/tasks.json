{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build MMOProject",
      "type": "shell",
      "command": "bash",
      "args": ["make.sh"],
      "problemMatcher": ["$gcc"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
